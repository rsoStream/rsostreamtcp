
package com.rsostream.tcp.server.health;
/*
import org.eclipse.microprofile.health.Health;
import org.eclipse.microprofile.health.HealthCheck;
import org.eclipse.microprofile.health.HealthCheckResponse;

import javax.enterprise.context.ApplicationScoped;
import java.util.logging.Logger;

@Health
@ApplicationScoped
public class CheckHealth implements HealthCheck {

    private static final Logger LOG = Logger.getLogger(CheckHealth.class.getSimpleName());

    @Override
    public HealthCheckResponse call() {
        return HealthCheckResponse.named(CheckHealth.class.getSimpleName()).up().build();
    }
}
*/
public class CheckHealth {

}